
# For Admin Login (replace with secure values or remove if not using basic auth)
# These are insecure and only for local demonstration.
# In a production environment, use a proper authentication system.
NEXT_PUBLIC_ADMIN_USERNAME="admin"
NEXT_PUBLIC_ADMIN_PASSWORD="password"

# Google Generative AI API Key (for Genkit)
# Create one at https://aistudio.google.com/app/apikey
GOOGLE_GENAI_API_KEY="YOUR_GOOGLE_GENAI_API_KEY"

# MongoDB Connection URI
# Example: mongodb+srv://<username>:<password>@<cluster-url>/<database_name>?retryWrites=true&w=majority
# Ensure your database user has read/write permissions to the specified database.
MONGODB_URI="YOUR_MONGODB_CONNECTION_STRING"
